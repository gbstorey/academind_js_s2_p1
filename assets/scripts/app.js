const defaultResult = 0;
let curResult = defaultResult;
let calculationDescription = `(${curResult} + 10) * 3 / 2 - 1`;
curResult = ((curResult + 10) * 3) / 2 - 1;
outputResult(curResult, calculationDescription);
